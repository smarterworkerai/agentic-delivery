# Input artifacts

## Issues and plans
- Issue: `<issue-url-or-number>`
- Plan: `<plan-url-or-path>`

## Relevant files or docs
- `<path-or-url>` — `<why it matters>`

## Copied artifacts
- None by default. Add exact copied files only when the delegated task truly requires them.

## Context policy

Prefer summarized markdown context over large raw file copies. Redact secrets as `[REDACTED]` if excerpts are necessary.
