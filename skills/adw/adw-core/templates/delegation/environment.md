# Environment

- Portable contract: `adw-delegation-v1`
- Selected backend/target: `<backend-or-target>`
- Model/agent/runtime: `<model-or-agent>`
- Repository checkout location: `<backend-specific-or-local-path-if-needed>`
- Delegation run root: `<backend-specific-run-root-if-needed>`

## Backend assumptions

<Describe only the assumptions needed by the selected worker. Keep backend-specific launcher commands, credentials, host/user details, path mappings, permission rules, and cache paths in backend-owned docs or handoff notes, not in portable ADW skill docs.>
